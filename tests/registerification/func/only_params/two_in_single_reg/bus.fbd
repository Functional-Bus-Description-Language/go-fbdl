# Test checking scenario when there are only params, no returns.
# The params occupy exactly one register.
# It also checkes whether element after the func has proper address.
Main bus
	F func
		p1 param; width = 10
		p2 param; width = 22
	St status

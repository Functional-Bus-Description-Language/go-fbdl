# Test checking whether status right after the func gets new address when there are only returns and there is enough space for the status.
# In such case status cannot be put into the func register as this would lead to spurious function acknowledgements during status read.
Main bus
	F func
		r return; width = 8
	S status; width = 4

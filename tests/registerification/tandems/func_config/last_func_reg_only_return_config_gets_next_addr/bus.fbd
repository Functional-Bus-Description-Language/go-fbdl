# Config right after the func must get new address even if the last func register has only returns.
# In such case config cannot be put into the last func register as config is readable.
# A config read would lead to spurious acknowledgement generation.
Main bus
	F func
		p1 param; width = 30
		p2 param; width = 2
		r return; width = 8
	C config; width = 8
